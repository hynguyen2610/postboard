export interface Post { id: string; author: string; title: string; content: string; createdAt: number; commentCount: number; }
export interface Comment { id: string; postId: string; parentId: string | null; author: string; content: string; createdAt: number; depth: number; }
export interface CommentTree extends Comment { replies: CommentTree[]; }
type AddCommentResult = { comment: Comment } | { error: "not_found" | "bad_parent" | "max_depth" };
const authors = ["Mina Torres", "Jae Park", "Aisha Bello", "Tom Riley", "Yuki Sato", "Priya Nair", "Carlos Mendez", "Ingrid Holm", "Sam O'Neill", "Leila Farah", "Noah Kim", "Zara Khan", "Ben Alcott", "Fatima Rahman", "Oscar Lindqvist"];
const topics = [["Shipped the new caching layer", "Cut p95 latency on the feed endpoint from 420ms to 90ms by adding a request-scoped cache in front of the post repository. Writeup coming once the dashboards settle."], ["Anyone else fighting with cursor pagination?", "Offset pagination fell over once we hit a few million rows. Moved to keyset pagination on (created_at, id) and it's been steady since."], ["Small win: trimmed our bundle by 180kb", "Turned out we were shipping two date libraries. Removed one, swapped call sites, done in an afternoon."], ["Thoughts on optimistic UI for comments?", "Curious how folks handle rollback when a comment POST fails after the UI already shows it. Toast + remove, or silent retry?"], ["Postmortem: the Tuesday outage", "Root cause was a connection pool exhaustion under a traffic spike. Added backpressure and a circuit breaker, retro doc linked in the comments."], ["Three levels of comment nesting is plenty", "Tried unlimited nesting once. Nobody read past depth four and the UI turned into a hallway of arrows. Capping at three was the right call."], ["Weekend project: a tiny job queue", "Wrote a ~200 line job queue backed by an in-memory heap, mostly to understand backpressure and retries better."], ["Search relevance is harder than it looks", "Naive substring match gets you 80% of the way, then everyone wants typo tolerance and it turns into a whole project."], ["Load testing before the launch", "Ran a load simulation against the timeline endpoint this morning — seeded a couple thousand extra posts to see how pagination held up."], ["Note to self: index the search column", "Forgot to index the column we search on. Table scans on ten thousand rows really do add up."], ["Nice pattern for reply threads", "Storing depth directly on the comment row instead of computing it on read made the reply-limit check trivial."], ["Timeline feels snappier with load-more", "Swapped infinite scroll for an explicit load-more button. Less jank, and people seem to actually prefer knowing where the bottom is."], ["Debouncing search input", "300ms debounce plus an abort controller for in-flight requests cleared up a bunch of flicker on fast typing."], ["Why we keep the demo data obviously fake", "Made every seeded post clearly synthetic so nobody mistakes the load test for real user content."], ["A short note on empty states", "An empty search result should tell you what happened and what to try next, not just show a blank page."]];
const randomFrom = <T,>(list: readonly T[]): T => list[Math.floor(Math.random() * list.length)]!; const makeId = (prefix: string) => `${prefix}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 9)}`;
export class Store { private posts = new Map<string, Post>(); private comments = new Map<string, Comment>(); private postOrder: string[] = []; addPost({ author, title, content }: { author?: string; title: string; content?: string }): Post { const post: Post = { id: makeId("post"), author: author?.trim() || "Anonymous", title: title.trim() || "(untitled)", content: content?.trim() || "", createdAt: Date.now(), commentCount: 0 }; this.posts.set(post.id, post); this.postOrder.unshift(post.id); return post; } seedPosts(count: number, { spreadDays = 30 }: { spreadDays?: number } = {}): number { const now = Date.now(), created: Post[] = []; for (let i = 0; i < count; i++) { const [title, content] = randomFrom(topics); const post: Post = { id: makeId("post"), author: randomFrom(authors), title, content, createdAt: now - Math.random() * spreadDays * 86_400_000, commentCount: 0 }; this.posts.set(post.id, post); created.push(post); } this.postOrder = [...this.postOrder, ...created.map((post) => post.id)].sort((a, b) => this.posts.get(b)!.createdAt - this.posts.get(a)!.createdAt); return created.length; } countPosts() { return this.postOrder.length; } listPosts({ q = "", cursor = 0, limit = 10 }: { q?: string; cursor?: number; limit?: number }) { let ids = this.postOrder; if (q.trim()) { const needle = q.trim().toLowerCase(); ids = ids.filter((id) => { const post = this.posts.get(id)!; return post.title.toLowerCase().includes(needle) || post.content.toLowerCase().includes(needle) || post.author.toLowerCase().includes(needle); }); } return { posts: ids.slice(cursor, cursor + limit).map((id) => this.posts.get(id)!), nextCursor: cursor + limit < ids.length ? cursor + limit : null, total: ids.length }; } getPost(id: string) { return this.posts.get(id) ?? null; } addComment({ postId, parentId, author, content }: { postId: string; parentId?: string | null; author?: string; content: string }): AddCommentResult { const post = this.posts.get(postId); if (!post) return { error: "not_found" }; let depth = 1; if (parentId) { const parent = this.comments.get(parentId); if (!parent || parent.postId !== postId) return { error: "bad_parent" }; if (parent.depth >= 3) return { error: "max_depth" }; depth = parent.depth + 1; } const comment: Comment = { id: makeId("cmt"), postId, parentId: parentId ?? null, author: author?.trim() || "Anonymous", content: content.trim(), createdAt: Date.now(), depth }; this.comments.set(comment.id, comment); post.commentCount++; return { comment }; } getCommentTree(postId: string): CommentTree[] { const all = [...this.comments.values()].filter((comment) => comment.postId === postId).sort((a, b) => a.createdAt - b.createdAt); const byId = new Map(all.map((comment) => [comment.id, { ...comment, replies: [] as CommentTree[] }] as const)); const roots: CommentTree[] = []; for (const comment of byId.values()) { if (comment.parentId && byId.has(comment.parentId)) byId.get(comment.parentId)!.replies.push(comment); else roots.push(comment); } return roots; } }
export const store = new Store(); store.seedPosts(24, { spreadDays: 10 });
