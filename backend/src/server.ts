import express, { type Response } from "express"; import cors from "cors"; import sharp from "sharp"; import { store } from "./store.js";
const app = express(); app.use(cors()); app.use(express.json()); const PORT = Number(process.env.PORT) || 4000; const baseUrl = "https://raw.githubusercontent.com/yavuzceliker/sample-images/main/docs"; const namePattern = /^image-([1-9]\d{0,2}|1\d{3}|2000)\.jpg$/; type ImageWidth = 320 | 640 | 1280; type ImageFormat = "avif" | "webp"; const widths = new Set<ImageWidth>([320, 640, 1280]), formats = new Set<ImageFormat>(["avif", "webp"]), cache = new Map<string, { body: Buffer; contentType: `image/${ImageFormat}` }>(); function send(res: Response, derivative: { body: Buffer; contentType: `image/${ImageFormat}` }) { res.set({ "Cache-Control": "public, max-age=31536000, immutable", "Content-Type": derivative.contentType }).send(derivative.body); } async function simulateReadLoad() { const delay = Math.min(500, Math.floor(store.countPosts() / 8)); if (delay) await new Promise<void>((resolve) => setTimeout(resolve, delay)); }
app.get("/api/health", (_req, res) => res.json({ ok: true, posts: store.countPosts() })); app.get("/api/images/:name", async (req, res) => { const name = req.params.name, width = Number(req.query.width), format = req.query.format; if (!namePattern.test(name) || !widths.has(width as ImageWidth) || typeof format !== "string" || !formats.has(format as ImageFormat)) return res.status(400).json({ error: "invalid image derivative request" }); const typedWidth = width as ImageWidth, typedFormat = format as ImageFormat, key = `${name}:${typedWidth}:${typedFormat}`, cached = cache.get(key); if (cached) return send(res, cached); try { const source = await fetch(`${baseUrl}/${name}`); if (!source.ok) throw new Error(`upstream image request failed (${source.status})`); const body = await sharp(Buffer.from(await source.arrayBuffer())).rotate().resize({ width: typedWidth, fit: "inside", withoutEnlargement: true }).toFormat(typedFormat, typedFormat === "avif" ? { quality: 50, effort: 4 } : { quality: 72 }).toBuffer(); const derivative = { body, contentType: `image/${typedFormat}` as const }; cache.set(key, derivative); if (cache.size > 128) cache.delete(cache.keys().next().value!); return send(res, derivative); } catch (error) { console.error("Unable to create image derivative", { name, width, format, error: error instanceof Error ? error.message : String(error) }); return res.status(502).json({ error: "image source unavailable" }); } }); app.get("/api/posts", async (req, res) => { await simulateReadLoad(); const q = typeof req.query.q === "string" ? req.query.q : "", cursor = Number.parseInt(String(req.query.cursor), 10) || 0, limit = Math.min(50, Number.parseInt(String(req.query.limit), 10) || 10); res.json(store.listPosts({ q, cursor, limit })); }); app.post("/api/posts", (req, res) => { const { author, title, content } = req.body as { author?: unknown; title?: unknown; content?: unknown }; if (typeof title !== "string" || !title.trim()) return res.status(400).json({ error: "title is required" }); res.status(201).json({ post: store.addPost({ author: typeof author === "string" ? author : undefined, title, content: typeof content === "string" ? content : undefined }) }); }); app.get("/api/posts/:id", (req, res) => { const post = store.getPost(req.params.id); return post ? res.json({ post }) : res.status(404).json({ error: "not_found" }); }); app.get("/api/posts/:id/comments", (req, res) => store.getPost(req.params.id) ? res.json({ comments: store.getCommentTree(req.params.id) }) : res.status(404).json({ error: "not_found" })); app.post("/api/posts/:id/comments", (req, res) => { const { author, content, parentId } = req.body as { author?: unknown; content?: unknown; parentId?: unknown }; if (typeof content !== "string" || !content.trim()) return res.status(400).json({ error: "content is required" }); const result = store.addComment({ postId: req.params.id, author: typeof author === "string" ? author : undefined, content, parentId: typeof parentId === "string" ? parentId : null }); if ("error" in result) return res.status(result.error === "not_found" ? 404 : 400).json({ error: result.error }); return res.status(201).json(result); }); app.post("/api/simulate-load", (req, res) => { const parsed = Number.parseInt(String((req.body as { count?: unknown })?.count), 10), count = Math.min(5000, Math.max(1, parsed || 500)), started = Date.now(), added = store.seedPosts(count); res.json({ added, tookMs: Date.now() - started, totalPosts: store.countPosts() }); }); app.listen(PORT, () => console.log(`Postboard API listening on http://localhost:${PORT}`));
