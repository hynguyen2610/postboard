import { useState, type FormEvent } from "react";
import { createComment } from "../api";
import type { Comment } from "../types";
export default function CommentForm({ postId, parentId = null, placeholder = "Add a comment", onAdded, onCancel }: { postId: string; parentId?: string | null; placeholder?: string; onAdded: (comment: Comment) => void; onCancel?: () => void }) { const [author, setAuthor] = useState(""), [content, setContent] = useState(""), [submitting, setSubmitting] = useState(false), [error, setError] = useState<string | null>(null); async function handleSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); if (!content.trim()) return setError("Write something before posting."); setSubmitting(true); setError(null); try { const { comment } = await createComment(postId, { author, content, parentId }); setContent(""); onAdded(comment); } catch (err) { setError(err instanceof Error ? err.message : "Unable to post comment"); } finally { setSubmitting(false); } } return <form className="comment-form" onSubmit={handleSubmit}><div className="comment-form-row"><input className="comment-form-author" placeholder="Name (optional)" value={author} onChange={(e) => setAuthor(e.target.value)} /></div><textarea className="comment-form-body" placeholder={placeholder} rows={2} value={content} onChange={(e) => setContent(e.target.value)} autoFocus />{error && <p className="form-error">{error}</p>}<div className="comment-form-actions">{onCancel && <button type="button" className="btn btn-quiet btn-small" onClick={onCancel}>Cancel</button>}<button type="submit" className="btn btn-primary btn-small" disabled={submitting}>{submitting ? "Posting…" : "Post"}</button></div></form>; }
